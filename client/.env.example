# Leave empty to use same-origin relative /api requests (recommended with the Vite dev proxy).
# Set to an absolute origin when the API is deployed separately, e.g. https://api.octatrade.example
VITE_API_BASE_URL=
